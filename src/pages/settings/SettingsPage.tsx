// Settings page — placeholder. Will house: auth, agent connection, theme, extension lint.

export function SettingsPage() {
  return (
    <div className="flex h-full flex-col gap-4 p-6">
      <h1 className="text-base font-semibold">Settings</h1>
      <p className="text-sm text-muted-foreground">
        Agent connection, authentication, theme, and extension management — Milestone 4+.
      </p>
    </div>
  );
}

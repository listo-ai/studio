// Flows page — placeholder for Milestone 5 (React Flow canvas).
// Wire up @acme/agent-client.nodes queries here once M4 (auth + transport) lands.

export function FlowsPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-2 text-muted-foreground">
      <p className="text-sm">Flows canvas — Milestone 5</p>
      <p className="text-xs">React Flow + node palette + property panel</p>
    </div>
  );
}
